
import { StyledLoader } from '@/components/styled-components';

export const Loader = () => {
    return (
        <StyledLoader data-testid='animation-loader'>
            <span></span>
        </StyledLoader>
    )
}
